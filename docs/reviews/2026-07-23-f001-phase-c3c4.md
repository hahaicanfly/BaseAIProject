# F-001 Phase C3(抗幻覺)+ C4(範圍/策略/迴圈) — 驗收報告

> Fresh-context 驗收代理,依 model-dispatch.md §5 執行。未參與實作,一切以檔案現狀與實際執行為準。
> Repo: `/Users/a17/AIproject/BaseAIProject`,分支 `feat/harness-verify-clarify-gates`。
> 驗收全程僅在 scratchpad 自建沙箱操作 hook/腳本;`git status --porcelain` 於驗收結束時與開始時逐行一致,確認無自我指涉噪音、無誤觸真實 repo 檔案。

## 1. 變更集比對 — **PASS**

`git status --porcelain | sort` 實際輸出:40 個 `M`、7 個 `??`(6 個宣告項 + 使用者的「AI 基礎架構優化目標說明.md」)、2 個 rename(`agent_docs/zh/commands/` 兩檔去 `_zh` 後綴)。逐檔核對與宣告清單完全一致,無多無少。

唯一觀察:第二個 rename 為 `RM`(rename + 內容修改)而非純 `R `,即 `last-word_zh.md → last-word.md` 重新命名後內容也被改動;`techdebt_zh.md → techdebt.md` 則是純 `R `。宣告文字只寫「2 個 R」未區分 R/RM,判定仍屬同一操作(zh 命令檔去後綴),非額外變更 → 不影響本項判定。

## 2. 編譯與機械閘門 — **PASS**

```
$ python3 -m py_compile .claude/hooks/*.py scripts/*.py   → exit 0, 無輸出
$ python3 scripts/acceptance-run.py docs/plans/active/F-001-harness-verifiability-batch.md
PASS build / PASS lint-settings / PASS plan-lint / PASS negative-lint
Summary: 4 total, 4 pass, 0 fail, 0 skip
$ python3 scripts/check-doc-refs.py --all 2>/dev/null | tail -1
SUMMARY: 0 ERROR, 10 WARN (R1=2 R2=0 R3=8 R4=0) across 150 file(s)
$ python3 scripts/retro-status.py
retro-status: ERRORS.md 134/300 行 | Pending 10/20 條 | CLAUDE.md 87/100 行 | rules 523/600 行 | ...
```

手算核對:`wc -l docs/learnings/ERRORS.md` = 134(相符)、`wc -l CLAUDE.md` = 87(相符)、`wc -l .claude/rules/*.md` 總計 = 523(相符,且確認 `.claude/rules/*.md` glob 不含 `CLAUDE.md`,符合 §5 字面定義)。讀 `scripts/retro-status.py` 原始碼確認邏輯與輸出一致,非寫死數字。

## 3. 哨兵套件(b-smoke,30 項)— **PASS**

```
$ cd scratchpad/b-smoke && cp .../stop-retro-logger.py .../_lib.py repo/.claude/hooks/ && python3 run-tests.py
PASS T1...T12(缺T9,11項) / PASS B1...B10(10項) / PASS V1-V3(3項) / PASS X1-X4(4項) / PASS Y1-Y2(2項)
ALL PASS  (11+10+3+4+2 = 30)
```

讀 `run-tests.py` 原始碼確認 X1、Y2 非恆真斷言:
- X1(`UNVERIFIED_CITATION` for fabricated URL)與 X2(已 fetch 的 URL 不告警)、X3(白名單 host 不告警)成對出現,證明偵測邏輯有實際分支,非永遠命中。
- Y2 檢查 `metrics-monthly.jsonl` 實際內容(`month=="2026-06"`、`count==1`)且確認舊事件真的從 `hook-events.jsonl` 移除,非空殼斷言。

## 4. C3 行為抽驗(獨立沙箱直測,非沿用 b-smoke)— **PASS**

於 `scratchpad/c3c4-direct` 自建乾淨沙箱(獨立複製 `stop-retro-logger.py` + `_lib.py`),不依賴實作方準備好的測試資料:

- 構造 transcript,report 內文引用一個從未在 transcript 任何 tool_use/text 出現過的 URL(`https://never-seen-anywhere-else.invalid/report`)→ 執行後 `docs/learnings/ERRORS.md`(沙箱)出現 `[UNVERIFIED_CITATION]` 條目,含該 URL,exit 0。
- 負向對照:同結構但改為 `WebFetch` 工具實際抓取過的 URL → 無 `UNVERIFIED_CITATION` 產生,ERRORS.md 維持空白。確認偵測是「有無在 transcript 中被 fetch/observe 過」而非恆真觸發。
- 構造 transcript 含 `[RULE_FIRED: clarify-first|target=missing]` → 執行後 `state/rule-events.jsonl`(沙箱)新增一筆,`kind=="RULE_FIRED"`,`detail` 含 `clarify-first`。

## 5. post-edit-lint 兩個新哨兵黑箱測試 — **PASS**

於 `scratchpad/pel-test` 自建獨立沙箱 repo(含 `.claude/hooks/`、`.claude/rules/*.md`(真實內容,合計 523 行 < 600)、`scripts/check-doc-refs.py`):

- 建立含壞引用的 `docs/testbadref.md`(backtick 路徑指向不存在檔案)→ 用 PostToolUse 風格 payload(`tool_name: Write`, `file_path: docs/testbadref.md`)呼叫 `post-edit-lint.py` → stderr 印出 `dead doc reference(s) in docs/testbadref.md: ERROR ... R1 ...`,`state/hook-events.jsonl` 新增 `reason: DOC_REF` 的 warn 事件,exit 0。
- 對 `.claude/rules/security.md` 送出 Edit payload(此時沙箱 rules 總量 523 < 600)→ `state/hook-events.jsonl` 中無任何 `RULE_BUDGET` 事件,確認閾值以下不告警(僅出現與本測試無關的既有 `INV-SEC-002` 掃描噪音,exit 仍為 0,不影響判定)。

## 6. 文件連貫抽驗(a)-(j) — **PASS**(1 項非阻塞觀察)

| 項 | 結果 | 證據 |
|---|---|---|
| (a) handoff-protocol EN/zh UNCONFIRMED+Telemetry | PASS | 兩檔第 106/110/113/121 行結構完全對應 |
| (b) review-protocol EN/zh `[SCOPE]` 反向核對 + Document Reviewer Checklist | PASS | 兩檔第 55、100 行對應 |
| (c) execplan-lifecycle EN/zh Scope Change 節 + Phase 1/6 更新 | PASS | Scope Change 節(132行起)、Phase 1 Exit 已含 Scope Baseline、Phase 6 Action 已含 Non-Goals 反查,EN/zh 逐行對應 |
| (d) PLANS.md/zh §1 模板 Clarify-first + Scope Baseline | PASS | 兩檔 42-43 行(模板)、180-181 行對應 |
| (e) delegation-templates EN/zh §7 存在且 §1-§6 未破壞 | PASS | 兩檔皆有 §1-§7 七節,§6 內容完整未被截斷 |
| (f) 4 個研究 agent(competitive-analyst/data-analyst/market-researcher/pm)EN/zh 皆含「假設-證據表」 | PASS(見下方觀察) | 四檔 EN/zh 皆有 `### 假設-證據表` |
| (g) PDR-template.md 8 段齊 | PASS | §1-§8 全部存在(決策問題與背景／選項比較表／採用決策／證據清單／關鍵假設／可證偽成功指標／Non-Goals／狀態) |
| (h) CLAUDE.md 87 行 / CLAUDE_zh.md 86 行(≤100)+ scripts/ 工具列 | PASS | `wc -l` 確認 87/86;兩檔第 67 行皆有 `scripts/`(acceptance-run/execplan-lint/check-doc-refs/retro-status)列 |
| (i) ERRORS.md Pending Review 含 harness-maintenance §6 提案 | PASS | 第 90-94 行,位於 `## Pending Review`(25-95 行)區間內 |
| (j) execplan-lint exit 0 + F-001 C3/C4 checkbox 全 `[x]` | PASS | C3a-C3e、C4a-C4d 共 9 項全部 `[x]`;lint 僅 1 條 W1 warning(§1 缺 Clarify-first 行,屬既有格式警告非本次範圍),exit 0 |

**非阻塞觀察(f)**:四個研究 agent 的 **EN 版**(`.claude/agents/*.md`)裡,`### 假設-證據表` 這一節標題與表頭欄位是直接以繁體中文寫入,而同檔案其餘欄位(如 `### Competitive Comparison Report Template`、`### Output Format`)皆為英文。這與既有「EN 為正典、zh 為人讀鏡像」的分工不一致——EN canonical 檔案本身混入未翻譯的中文段落。字面驗收標準(「EN/zh 都含」)本身成立,故不構成本項 FAIL,但建議後續把 EN 檔案的此節標題/表頭改為英文,由 zh 鏡像檔翻譯呈現,以維持 EN/zh 分工的結構一致性。

## 7. Red-tier 合規抽查 — **PASS**

`git diff --name-only` 與 `git status --porcelain` 均未出現 `harness-maintenance.md`;其 §6 修改提案已正確依 §8 Red-tier 自我修改禁令,轉為 ERRORS.md Pending Review 的人審條目(見第 6 項 (i))。

## 總結

7 大項全數 PASS,僅第 6 項有 1 則非阻塞觀察(EN 研究 agent 檔案混入未翻譯中文小節,建議後續整理,不影響本次交付判定)。未發現任何機械閘門失敗、哨兵測試失敗、範圍蔓延、或 Red-tier 違規。

**VERDICT: PASS**
