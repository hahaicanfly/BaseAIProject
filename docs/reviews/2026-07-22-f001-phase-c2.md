# F-001 Phase C2 (可驗證性主線) — 驗收報告

> Fresh-context 驗收代理,依 model-dispatch.md §5 執行。未參與實作,一切以檔案現狀與實際執行為準。
> Repo: `/Users/a17/AIproject/BaseAIProject`,分支 `feat/harness-verify-clarify-gates`。

## 1. `git status --porcelain` 變更集比對 — **FAIL**

實際輸出:
```
 M .claude/hooks/pre-compact-snapshot.py
 M .claude/hooks/stop-retro-logger.py
 M .claude/protocols/review-protocol.md
 M .claude/settings.json
 M .claude/templates/delegation-templates.md
 M docs/learnings/ERRORS.md
 M docs/plans/PLANS.md
 M docs/plans/PLANS_zh.md
 M docs/plans/active/F-001-harness-verifiability-batch.md
 M state/SCHEMA.md
?? .claude/hooks/delegation-ledger.py
?? .github/
?? "AI 基礎架構優化目標說明.md"
?? docs/reviews/
?? scripts/
```

- 宣告的 9 個 M 檔案全部存在,無缺漏。
- `??` 未追蹤集合與宣告的 5 項完全一致,無缺漏、無多餘。
- **多出 1 項未宣告的 M 檔案:`docs/learnings/ERRORS.md`。** `git diff docs/learnings/ERRORS.md` 顯示只新增了一段 `[PR_RETRO]` harvest 條目(`<!-- harvest:cf9ceaf881 -->`,session `077d3a3f-6205-409d-99de-bf82c10d766e`,即本次驗收 session 本身),內容是 stop-retro-logger.py 對本 session 先前 SubagentStop 事件的正常收割副作用,非 Phase C2 實作內容變更。性質判斷為低風險的自我指涉噪音,而非範圍蔓延,但依驗收字面標準(「恰為」)仍構成偏差,必須如實回報,不可自行判定「應該沒關係」而放行。

**結論:變更集不完全等於宣告清單,多 1 個未宣告 M 檔案 → 本項 FAIL。**

## 2. `py_compile` — **PASS**

```
$ python3 -m py_compile .claude/hooks/*.py scripts/*.py
(exit 0, no output)
```

`git diff .claude/hooks/pre-compact-snapshot.py` 確認括號修復存在:
```diff
-        for marker in ("[HUMAN_ATTENTION_REQUIRED:", "[VERIFY_FAILED:", "[HANDOFF:"]:
+        for marker in ("[HUMAN_ATTENTION_REQUIRED:", "[VERIFY_FAILED:", "[HANDOFF:"):
```
list `[...]` 已改為 tuple `(...)`,語法錯誤修復屬實。

## 3. 哨兵套件(b-smoke,24 項) — **PASS**

```
$ cd .../scratchpad/b-smoke && cp .../stop-retro-logger.py .../_lib.py repo/.claude/hooks/ && python3 run-tests.py
PASS T1 ... PASS T12  (11 項 R1 regression)
PASS B1 ... PASS B10  (10 項 semantic/fence/git-commit/flock)
PASS V1 VERDICT PASS recorded
PASS V2 VERDICT FAIL recorded+pending
PASS V3 Stop no verdict harvest
ALL PASS
```
共 24 項,含 V1-V3 VERDICT 收割測試,全 PASS。

## 4. `scripts/acceptance-run.py` — **PASS**

```
$ python3 scripts/acceptance-run.py docs/plans/active/F-001-harness-verifiability-batch.md
PASS build ...
PASS lint-settings ...
PASS plan-lint ...
PASS negative-lint ...
Summary: 4 total, 4 pass, 0 fail, 0 skip
EXIT:0
```
`state/acceptance/F-001-harness-verifiability-batch.jsonl` 末 3 行 `ts: 2026-07-22T15:37:15Z` 對應本次執行,含 label/cmd/exit_code/pass 欄位,證實本次執行有落地紀錄。

## 5. `scripts/execplan-lint.py` — **PASS**

```
$ python3 scripts/execplan-lint.py docs/plans/active/F-001-harness-verifiability-batch.md
WARN docs/plans/active/F-001-harness-verifiability-batch.md: W1 §1 Goal lacks a 'Clarify-first:' line
EXIT:0
```
exit 0,唯一輸出是可接受的 W1 警告。

## 6. `.claude/settings.json` 三組新佈線 — **PASS**

`python3 -m json.tool .claude/settings.json` 通過(JSON 合法)。內容確認三組佈線皆存在:
- `PreToolUse` matcher `"Task|Agent"` → `delegation-ledger.py`(settings.json:14-22)
- `PostToolUse` matcher `"Bash"` → `post-bash-commit-ledger.py`(settings.json:34-42)
- `SessionStart` → `session-activation-check.py`(settings.json:44-54)

## 7. `delegation-ledger.py` 沙箱抽測(2 案) — **PASS**

沙箱:`.../scratchpad/item7/sandbox/.claude/hooks/{delegation-ledger.py,_lib.py}`(REPO_ROOT 隨檔案位置解析,天然隔離於真實 repo state)。

```
$ python3 test_delegation_ledger.py
PASS CaseA trio-flags all true + exit0
PASS CaseB Bash tool not recorded + exit0
ALL PASS
```
- Case A:完整三要素(goal/acceptance/report-format)+ scope + marker 提示的 prompt → ledger 記一筆,`has_goal`/`has_ac`/`has_report_format` 皆 `true`,exit 0。
- Case B:`tool_name: "Bash"` → ledger 檔案未產生任何記錄,exit 0。

## 8. `.github/workflows/harness-gates.yml` — **PASS**

```python
import yaml
yaml.safe_load(open(".../harness-gates.yml"))
# jobs = ['py-compile', 'secret-scan', 'execplan-lint', 'placeholder-gate']
```
YAML 可解析,4 個 job 皆存在(py-compile / secret-scan / execplan-lint / placeholder-gate)。

## 9. `docs/plans/PLANS.md` 與 `PLANS_zh.md` 模板結構 — **PASS**

兩檔案 fence 結構完全一致(逐行比對):
```
27:````markdown   (開,§2. Required Sections 模板)
59:```acceptance  (巢狀開)
64:```            (巢狀關)
84:````           (關)
92:```  / 112:```  (無關的 lifecycle 流程圖區塊,非模板)
163:````markdown   (開,§7. Template (copy and use))
197:```acceptance  (巢狀開)
202:```            (巢狀關)
218:````           (關)
```
- 兩個 ````markdown 外層圍欄成對、各自內含 ```acceptance 巢狀區塊 — 確認。
- §2「Required Sections」模板內(PLANS.md:42 / PLANS_zh.md:42),`## 1. Goal` 子節含 `Clarify-first:` 行 — 確認(§7 copy-template 內 PLANS.md:179 / PLANS_zh.md:179 同樣含該行)。
- §2 模板塊(關閉圍欄 PLANS.md:84)後緊接 acceptance 機器執行說明段(PLANS.md:86 / PLANS_zh.md 對應行):「§5 acceptance block is machine-run: `python3 scripts/acceptance-run.py <plan.md>` ...」— 確認。
- PLANS_zh.md 為對應中文鏡像,內容與結構一致(已用 grep/sed 核對關鍵行)。

## 10. `delegation-templates.md` §6 / `review-protocol.md` 檢查表 — **PASS**

- `delegation-templates.md:127-132` 含「Verdict persistence」段:要求把完整報告 Write 到 `docs/reviews/<file>.md`,且最終訊息含 `VERDICT: PASS|FAIL docs/reviews/<file>.md` 行(stop-retro-logger 收割進 `state/verifications.jsonl`,FAIL 另落 ERRORS.md Pending Review)。
- `review-protocol.md:56` 檢查表含:「Run `python3 scripts/acceptance-run.py <execplan-path>` — it executes §5's ```acceptance block ... paste its summary line in the report.」

## 11. `state/SCHEMA.md` 4b/4c/4d 三節與實作欄位比對 — **PASS**

| 節 | SCHEMA.md 欄位 | 實作 | 欄位比對 |
|----|----------------|------|----------|
| 4b delegations(SCHEMA.md:144-154) | ts, session, subagent_type, model, desc, prompt_sha1_10, prompt_chars, has_goal, has_ac, has_report_format, has_scope, has_marker_req | `delegation-ledger.py:94-107` record dict | 完全一致 |
| 4c verifications(SCHEMA.md:158-168) | ts, session, agent, verdict, evidence_path | `stop-retro-logger.py:719-726` VERIFICATIONS_LEDGER append | 完全一致 |
| 4d acceptance(SCHEMA.md:172-182) | ts, plan, label, cmd, expect_fail, exit_code, pass, skipped, output_tail | 第 4 項實測 jsonl 實際輸出欄位 | 完全一致 |

三節皆存在,欄位名逐一比對後與實作完全相符,無缺漏、無多餘、無改名不同步。

## 12. `docs/reviews/README.md` — **PASS**

檔案存在(`docs/reviews/README.md`),內容說明:檔名慣例 `<YYYY-MM-DD>-<slug>.md`、內容格式(逐項 criterion → PASS/FAIL → evidence)、`VERDICT: PASS|FAIL <path>` 由 stop-retro-logger.py 機器收割進 `state/verifications.jsonl`、FAIL 額外落 ERRORS.md Pending Review。VERDICT 慣例描述完整。

---

## 總結

12 項中 11 項 PASS,1 項(#1 git status 變更集比對)FAIL——差異僅為 `docs/learnings/ERRORS.md` 多出 1 個未宣告的 M(內容是本次驗收 session 自身觸發的 `[PR_RETRO]` 收割副作用,非 Phase C2 實作內容),其餘 9 個宣告的 M 與 5 個宣告的 `??` 皆精確吻合、無其他偏差。功能面(hooks、腳本、CI workflow、模板、SCHEMA 文件、docs/reviews 慣例)全數通過機器可驗證的驗收標準。

**建議**:在合併前,將 `docs/learnings/ERRORS.md` 這行 harvest 從本次 diff 中排除(例如 `git restore --staged`/切分 commit)或明確加入宣告清單,以維持變更集與宣告的一致性;這不影響 Phase C2 的實作邏輯正確性,純屬變更集衛生問題。

VERDICT: FAIL docs/reviews/2026-07-22-f001-phase-c2.md
