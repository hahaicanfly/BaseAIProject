## Harness Maturity Report — /Users/a17/AIproject/BaseAIProject
**Date**: 2026-07-21
**Total score**: 90 / 100 → Level 4 [Self-Monitoring]

Rubric cross-check (`.claude/skills/harness-eval/rubric.md`): all eight per-dimension scores were re-derived from the rubric's own point tables and match exactly (D1 3+1+3+2+5=14, D2 2+4+4=10, D3 4+3+2+2+3+3+3=20, D4 3+3+4+0=10, D5 2+2+0+2=6, D6 2+5+2+2+4=15, D7 1+3+4+2=10, D8 2+2+1=5). One judgment call worth flagging, not an error: D1's "role definition" line in the rubric only states `present=2`, with no explicit partial-credit clause; the scan awarded 1/2 for a section that is present but split across an unfilled `{{tagline}}`/`{{Tech Stack}}` placeholder and behavioral (not identity) framing. This is consistent with how the rubric treats partial credit elsewhere (e.g. D4's `TODO only=1`), so it is left as scored rather than rounded up to 2. Sum = 90, which lands in the L4 (81–95) "Self-Monitoring" band, one tier below the top L5 "SkillOpt-Ready" band (96–100).

### Score Card

| Dimension | Score | Max | Comment |
|---|---|---|---|
| D1 — Constitutional Layer | 14 | 15 | Genuinely minimal, tiered-reading CLAUDE.md with a real canon-hierarchy conflict rule and 6 substantive rule files; loses 1 pt because the role/tagline slot (CLAUDE.md:3, :79-81) is still an unfilled `{{placeholder}}` — the repo is, by its own Activation Status rule, an unforked template. |
| D2 — Agent Coverage | 10 | 10 | 14 agent files (9 beyond the core 5), all 5 core roles (pm/architect/tech-lead/security-reviewer/qa-engineer) confirmed by filename AND frontmatter `name:`. Coverage is full; nothing mechanically forces the dispatch discipline that decides *when* they get used (that's prose in model-dispatch.md). |
| D3 — Hook System | 20 | 20 | All 4 hooks exist, are wired in settings.json, and `pre-tool-use-guard.py`'s exit-2 enforce logic is confirmed firing for real in `state/hook-events.jsonl` (17 live `enforced_block` events, e.g. a blocked `cat .env`). Strongest dimension in the repo — mechanical, not prose. |
| D4 — Invariants | 10 | 15 | GIT (5 rules) and SEC (real grep patterns) namespaces are solid and score full; project-specific breadth scores 0/5 — INV-TEST/API/ARC/BLD are all unfilled `> Fill in project X invariants.` placeholders, consistent with the unforked-template state. |
| D5 — ExecPlan System | 6 | 10 | PLANS.md (9-section spec) and the 10-phase lifecycle doc are well-designed and score full; loses all 4 pts on "≥1 completed ExecPlan" because `docs/plans/active/` and `completed/` contain nothing but their original `.gitkeep` — the system has never been used, ever, in this repo's git history. |
| D6 — Memory & Retro Loop | 15 | 15 | ERRORS.md (6 real dated lessons), SCHEMA.md, `state/.gitignore`, and 1518 real hook-events.jsonl lines (18 days, 3 hooks, incl. a real `enforced_block`) all present — the strongest evidence in the repo that a loop has run end-to-end in production, not just on paper. |
| D7 — Skills & Commands | 10 | 10 | 17 skill directories (8+ tier), key skills (code-review 72 lines, multi-agent-review 137 lines) are concrete and non-stub, `/last-word` is a real 8-step workflow. Trigger-vs-execution remains prose-judged (inherent to how Skills work, not a deduction here). |
| D8 — SkillOpt Loop Readiness | 5 | 5 | jsonl rollout log is real and mechanical (hooks write it, not just document it); ERRORS.md's Pending Review + Active Lessons dual structure is genuinely populated; skill-update trigger (pr-retro's "Case C") is explicitly defined. The loop is evidence-collecting and human-gated, not a closed automatic optimizer — that's expected at this maturity band, not a deduction. |
| **Total** | **90** | **100** | |

### Gap List (priority-ordered)

**[HIGH]**
1. Repo is still an unforked template — `CLAUDE.md:1` literally contains `{{PROJECT_NAME}}`, `invariants.md:1`'s title is `{{PROJECT_NAME}} — Mechanically Verifiable Invariants`; per the file's own Activation Status rule this caps how meaningful D1/D4's "constitutional" content can be in practice.
2. No mechanical gate blocks a "done" claim lacking verification evidence — judgment-rubrics.md §2/§5 and model-dispatch.md §5 ("never self-certified") are pure prose; `pre-tool-use-guard.py` (the only enforce-mode hook) has zero knowledge of whether a deliverable was verified.
3. The ExecPlan + review-protocol apparatus — the designed home for durable, structured verification records — has never been exercised: `docs/plans/active/` and `completed/` contain only `.gitkeep`, `state/feature-list.json` does not exist on disk. Zero real evidence this template gets filled with real data rather than left a stub.
4. The majority of real tasks (per plan-first.md's own <20-line/single-file/located-bug-fix exceptions) require no verification-evidence artifact at all — for those, "did I verify this" is whatever the model happens to say in chat.
5. CLAUDE.md's canonical decision tree (the single documented entry point) has no Step 0 for requirement clarification/scoping — it routes straight from "goal received" into ExecPlan or Plan Mode.
6. pm agent and spectra-amplifier skill (the two tools capable of producing scope/AC clarity) are both keyword/opt-in triggered — nothing routes a vague one-line request to either, and a plain request can bypass both.
7. All 14 agents run with `context_firewall:true` (non-interactive subagents) — so a written "Open Questions" field can never function as a live back-and-forth clarification gate; by the time anyone reads it, drafting has already happened on top of unverified assumptions.
8. Zero project-specific invariants exist — INV-TEST/API/ARC/BLD namespaces in `invariants.md` are all unfilled `> Fill in project X invariants.` placeholders (D4 scored 0/5 on this line).

**[MED]**
9. Most of the 6 `.claude/rules/` files are policy prose with no mechanical gate behind them — only the security/git-guard subset that overlaps `pre-tool-use-guard.py`'s regex patterns is actually enforced; model-dispatch tiering, judgment-rubrics escalation, plan-first, cost-optimization rely entirely on self-compliance.
10. Nothing mechanically forces the main conversation to actually delegate to pm/architect/security-reviewer/qa-engineer per model-dispatch.md's own prescriptions — dispatch discipline is self-policed prose (D2 coverage is full; D2 *usage* is unenforced).
11. `handoff-protocol.md:19` overstates what `stop-retro-logger.py` does — it claims a missing/malformed ending marker is "flagged," but the hook's `MARKER_RE` only matches markers that *are* present; there is no code path detecting absence, and `[HANDOFF:*]` is never parsed at all. A documentation/implementation mismatch on a claim central to the audit-trail story.
12. Fresh-context acceptance review (model-dispatch.md §5 / delegation-templates.md §6) is well-designed and covers documents and code equally, but has no invocation gate and no persistence requirement — its PASS/FAIL verdict can vanish with the subagent's ephemeral context.
13. `review-protocol.md` has detailed checklists for Code/Security/QA reviewers but none for non-code deliverables (design docs, PRDs, reports) — verification is implicitly code/test-only at the protocol-definition level.
14. INV-GIT-001, INV-GIT-005, and the entire TEST/API/ARC/BLD namespace have `HOOK: manual review` or no hook at all — prose-only, unenforced, distinct from the mechanically-gated INV-GIT-002/003/004 and INV-SEC-*.
15. The only structural ambiguity checkpoint in the ExecPlan lifecycle (plan-reviewer's Phase-3 "Open Questions unresolved" check) fires only *after* Phase 1 (pm requirements) and Phase 2 (architect design) have already converted the request into concrete decisions — reactive and late, not an upfront gate.
16. `docs/plans/PLANS.md` §2's 9-section ExecPlan template has no dedicated Non-Goals/Out-of-Scope field — even a fully rubric-compliant ExecPlan isn't required to state what's excluded. (Only feature-pipeline's Stage-1 wrapper has an Included/Excluded field, and it's not part of the base spec.)
17. judgment-rubrics.md §3's ambiguity circuit-break is reactive-only ("two reasonable interpretations" noticed mid-task) — no rule instructs a proactive pre-flight ambiguity scan before work starts.
18. No objective, checkable Signal→Action criterion exists anywhere for "this request is vague enough to require clarification" vs. "already scoped, proceed" — every other judgment point in judgment-rubrics.md has one; this decision point does not.
19. The ExecPlan lifecycle's two "human reviewer gate" checkpoints (`execplan-lifecycle.md:69`, `:111`) are enforced only by prose/agent self-compliance — the only hook that touches ExecPlans at all (`pre-compact-snapshot.py` via `_lib.py:116`) passively reads the active plan's path for session snapshots; it gates nothing.

**[LOW]**
20. `state/session-handoffs/` is empty despite 1518+ lines of `hook-events.jsonl` activity — `pre-compact-snapshot.py`'s snapshot-writing and the `/last-word` flow appear to have never produced a durable artifact in this repo's real usage; possible silent failure (the hook only logs write failures to jsonl, never surfaces them), unverified either way.
21. Even the (currently unused) ExecPlan template only asks §5 Verification Strategy to list command strings ("Build: `[build command]`"), not paste actual output — so a fully-used ExecPlan could still pass review on an unsubstantiated ✓.
22. `INV-SEC-003`'s git-add secret-file guard has a self-documented blind spot (`pre-tool-use-guard.py:94-99`): it cannot catch `git add -A`/`git add .` sweeping a secret file in incidentally, only literal filenames on the command line.
23. No `AskUserQuestion`-equivalent pattern is mandated anywhere in this project's own rules/protocols for vague requests — the pattern exists in the broader skill ecosystem (e.g. the generic `deep-research` skill's "ask 2-3 clarifying questions if underspecified") but has not been adopted into this harness's own idea-to-plan flow.
24. D8's SkillOpt loop is evidence-collecting and human-gated, not a closed-loop auto-optimizer — nothing currently reads `hook-events.jsonl` to automatically propose or apply a skill edit; a human/agent must execute pr-retro's Case C by hand.

### Focus Deep-Dive (1): Output Verifiability / Auditable Trail / Rollback

**What exists today**
- **Hard-enforced**: `pre-tool-use-guard.py` (`.claude/hooks/pre-tool-use-guard.py`) is the sole exit-2 blocking hook — it stops direct commits to master, force-push/reset-hard on master, reads of `.env`/secret files, `curl|sh`, `rm -rf /|~`. As a side effect it makes code changes rollback-safe by construction (everything is forced onto a feature branch + PR, hence revertible) — but it says nothing about whether a change is *correct*, only that it's *reversible*.
- **Hard-enforced (partial)**: `stop-retro-logger.py`'s marker-to-`ERRORS.md` harvest is genuinely working — real dated entries confirmed in `docs/learnings/ERRORS.md` from 2026-07-04/07-07 — but it only detects markers that *are* present in the transcript; it has no logic for detecting an *absent* marker, contradicting `handoff-protocol.md:19`'s claim that missing markers get "flagged."
- **Soft-policy**: `post-edit-lint.py`'s INV-SEC-001/002 scans (sentinel-only, never blocks per its own docstring), `model-dispatch.md` §5's "verification never self-certified" rule plus `delegation-templates.md` §6's fresh-context acceptance-review template (the one mechanism that treats documents and code as equally verifiable — but entirely elective and non-persistent).
- **Documented-only**: `review-protocol.md`'s structured Verification Results table (code/PR-scoped only, no equivalent for documents); `pre-compact-snapshot.py`'s session-snapshot design (wired in settings.json, but `state/session-handoffs/` is empty — never produced a real artifact); the entire ExecPlan §5/§6/§7/§9 verification-evidence apparatus and `state/feature-list.json`'s verification schema (neither has ever been instantiated — `docs/plans/active/` and `completed/` contain only `.gitkeep`); `/last-word`'s SESSION-HANDOFF.md production (CLAUDE.md calls it a "hard gate" but no hook checks it ran or validates its content).

**Gaps (ranked)**
1. [HIGH] No mechanical gate blocks a completion claim lacking verification evidence — the only enforce-mode hook has zero awareness of verification status.
2. [HIGH] The ExecPlan/review-protocol apparatus has never been exercised — 100% aspirational as deployed in this repo.
3. [HIGH] Most real tasks (plan-first.md's own exceptions) have no required verification artifact at all.
4. [MED] `handoff-protocol.md:19` overstates `stop-retro-logger.py`'s actual capability (documentation/implementation mismatch).
5. [MED] Fresh-context acceptance review has no invocation gate and no persistence requirement.
6. [MED] `review-protocol.md` has no reviewer checklist for non-code deliverables.
7. [LOW] ExecPlan §5 only requires command strings, not pasted output, even when used.
8. [LOW] INV-SEC-003's git-add guard can't catch `git add -A` sweeping in a secret file.
9. [LOW] `pre-compact-snapshot.py`'s safety net appears unused/unverified — echoes a prior "deployed but never smoke-tested" lesson already in ERRORS.md (2026-07-04).

**Concrete proposals**
- **missing-marker sentinel in `stop-retro-logger.py`** — extend the existing transcript-parsing pass to also check whether the final assistant text block ends with a required marker; if not (and `tool-calls.jsonl` shows edit activity for that session), log a `result="missing_marker"` event and append to `ERRORS.md` Pending Review under a new "Protocol Violations" bucket. Stays sentinel (non-blocking). *Plugs into*: `.claude/hooks/stop-retro-logger.py`, already wired to Stop/SubagentStop in settings.json. *Effort*: small (~half day).
- **`state/verification-log.jsonl` + Stop-time cross-check** — a mandatory lightweight record `{ts, session_id, task, deliverable_files[], verifier, evidence, pass_bool}` that a "done" claim should append; a Stop-time sentinel cross-checks session tool-calls against verification-log entries and flags the gap in ERRORS.md if edits happened with zero verification lines. *Plugs into*: new `state/verification-log.jsonl` (schema added to `state/SCHEMA.md`), hook logic in `stop-retro-logger.py` or a sibling hook, doc updates in `judgment-rubrics.md` §2 and `model-dispatch.md` §5. *Effort*: medium (~1 day).
- **Document/Report Reviewer Checklist in `review-protocol.md`** — add a checklist for non-code deliverables mirroring the existing Code/Security/QA sections, requiring a cited fresh-context acceptance-review PASS/FAIL before a document is marked reviewed; add a 6th criterion to judgment-rubrics.md §2 ("Actually Done"). *Plugs into*: `.claude/protocols/review-protocol.md` (new section) + `.claude/rules/judgment-rubrics.md` §2. *Effort*: small (documentation-only).
- **INV-VERIFY-001 + GitHub Actions companion check** — new invariant requiring every PR to link an ExecPlan whose §7 Decision Log and §5 Verification Strategy are non-stub (actual pasted output, not just command strings), enforced via a `.github/workflows/execplan-verification-check.yml` CI gate (local hooks can't gate a PR merge). *Plugs into*: `docs/architecture/invariants.md` (new INV-VERIFY-001) + new workflow file + `review-protocol.md` Code Reviewer checklist. *Effort*: medium (~1 day, plus needs a first real ExecPlan to test against since none exist yet).
- **`pre-compact-snapshot.py` self-check** — if hook-events.jsonl shows snapshot-write attempts but `state/session-handoffs/` has fewer files than expected, log a distinct warn event so this silent-failure class gets caught automatically rather than requiring a human to notice an empty directory. *Plugs into*: `.claude/hooks/pre-compact-snapshot.py` or `stop-retro-logger.py`. *Effort*: small (a few hours).

### Focus Deep-Dive (2): Requirement Clarification Before Planning

**What exists today**
- **Soft-policy**: the `pm` agent (`.claude/agents/pm.md`) produces a requirements doc with an "Open Questions" field, but it's keyword-triggered (需求/PRD/用戶故事/功能) and, because it runs as a non-interactive subagent (`context_firewall:true`), cannot pause mid-task to actually ask the user anything — Open Questions is a deferred written artifact, not a live gate. `judgment-rubrics.md` §3's ambiguity circuit-break exists but is reactive-only — it fires only once ambiguity is already noticed mid-task. `plan-reviewer`'s Phase-3 "Open Questions unresolved" check (`execplan-lifecycle.md`) is the one structural checkpoint touching ambiguity, but it fires after both requirements (Phase 1) and architecture (Phase 2) drafting are already done.
- **Documented-only**: `spectra-amplifier` (the most complete boundary-defining tool in the harness — explicit Out-of-Scope field, Given/When/Then ACs) is framed as "Recommended," not required, and is referenced nowhere in CLAUDE.md/plan-first.md/execplan-lifecycle.md/PLANS.md as a precondition for anything. CLAUDE.md's decision tree and plan-first.md's Workflow step 1 ("Analyze task requirements") give no structured method and no pointer to either tool.
- **Hard-enforced**: none. There is no mechanism anywhere in this harness that mechanically forces a clarification step before planning begins.

**Gaps (ranked)**
1. [HIGH] CLAUDE.md's canonical decision tree — the single documented entry point every session is told to consult — has no Step 0 for scope/requirement clarification.
2. [HIGH] pm agent and spectra-amplifier are both keyword/opt-in triggered; nothing routes a vague one-liner to either.
3. [HIGH] Agents run as non-interactive subagents (`context_firewall:true`), so Open Questions fields can't function as a live clarification loop — by the time anyone reads them, drafting has already happened on assumptions.
4. [MED] `PLANS.md` §2's 9-section template has no dedicated Non-Goals/Out-of-Scope field.
5. [MED] The only structural ambiguity check (plan-reviewer Phase 3) fires after two drafting stages have already occurred.
6. [MED] judgment-rubrics.md §3's ambiguity trigger is reactive-only; no proactive pre-flight equivalent exists.
7. [MED] No objective Signal→Action criterion exists for "vague enough to clarify" vs. "already scoped" — every other judgment point in judgment-rubrics.md has one; this one doesn't.
8. [LOW] No `AskUserQuestion`-equivalent pattern is mandated in this project's own rules/protocols (exists as precedent in the generic `deep-research` skill, not adopted here).

**Concrete proposals**
- **CLAUDE.md Decision-Tree Step 0: Clarify Scope** — insert a 1-2 line step before the existing 5-step tree: if the input is a raw goal (not already scoped), run a clarify pass (interactively, or via pm/spectra-amplifier) and get explicit user confirmation of scope before proceeding. *Plugs into*: `CLAUDE.md`, "Decision Tree Before Acting" section, before current step 1 (must stay within the file's self-imposed ≤100-line budget — trim elsewhere). *Effort*: small (<10 lines).
- **New standing rule `.claude/rules/clarify-first.md`** — defines an observable Signal→Action trigger in the same format as judgment-rubrics.md (e.g. trigger if 2+ of {target user, success metric, explicit boundaries, concrete trigger condition} are missing), specifies clarification must happen in the main conversation (not inside a `context_firewall:true` subagent) before Plan Mode/ExecPlan entry. *Plugs into*: new file, registered in CLAUDE.md's "Standing Rules" list alongside the existing 6. *Effort*: medium (~60-80 line file, modeled on judgment-rubrics.md).
- **Wire `plan-first.md` step 1 to `clarify-first.md`** — rewrite the current one-line "Analyze task requirements" step to explicitly apply the new signal checklist and require scope confirmation before drafting. *Plugs into*: `.claude/rules/plan-first.md`, Workflow section, step 1. *Effort*: small (<10 lines).
- **Add Non-Goals/Out-of-Scope as a required field** — add to `PLANS.md`'s template and to `pm.md`'s Output Format (currently only feature-pipeline's Stage-1 wrapper has an Included/Excluded field). *Plugs into*: `docs/plans/PLANS.md` §2 + `.claude/agents/pm.md` Output Format. *Effort*: small-medium (2-3 coordinated edits, <20 lines each).
- **Strengthen `execplan-lifecycle.md` Phase 1 exit criteria** — require "Goal filled in AND Non-Goals stated AND (if clarify-first.md fired) user confirmation quoted" before handoff to architect is permitted. *Plugs into*: `.claude/protocols/execplan-lifecycle.md`, Phase 1 — PROPOSED, Exit line. *Effort*: small (<10 lines).
- **plan-reviewer audits for a Clarification record, not just Open Questions** — since clarification quality is a judgment call (not hook-mechanizable per judgment-rubrics §6), extend plan-reviewer's Phase-3 checklist to verify a clarification record exists (signal didn't fire + why, or signal fired + confirmation link) — a post-hoc audit trail consistent with model-dispatch.md §5's fresh-context-review pattern. *Plugs into*: `.claude/agents/plan-reviewer.md` checklist + `PLANS.md` template near §8. *Effort*: medium (touches two files + needs a trigger-testing pass per harness-maintenance.md).

### Minimum Improvement Path (3 steps)

1. **CLAUDE.md Decision-Tree Step 0 + `clarify-first.md`** — cheapest, highest-leverage fix in the whole report: a <10-line edit to the single file every session is required to read, closing the top gap in Deep-Dive 2 (gaps #5-#7 in the merged list) without touching any hook or agent behavior. *Files*: `CLAUDE.md`, new `.claude/rules/clarify-first.md`.
2. **missing-marker sentinel in `stop-retro-logger.py`** — a half-day addition to an already-loaded, already-wired hook (no settings.json change needed) that makes `handoff-protocol.md:19`'s existing claim true instead of aspirational, and starts producing the first mechanical signal for "verification/handoff evidence is missing" (gap #2, #11 in the merged list). *File*: `.claude/hooks/stop-retro-logger.py`.
3. **Non-Goals/Out-of-Scope field in `PLANS.md` + `pm.md`** — a small, documentation-only edit (2-3 files, <20 lines each) that closes the scope-boundary gap for the one workflow path (ExecPlan) that does have real structure, at essentially zero implementation risk since no code/hook changes are involved. *Files*: `docs/plans/PLANS.md` §2, `.claude/agents/pm.md` Output Format.

### SkillOpt Readiness Index

D8 scores a clean 5/5, but the qualitative picture is "evidence-collecting and human-gated," not "closed-loop auto-optimizing" — which is exactly what's expected at L4 (Self-Monitoring) rather than L5 (SkillOpt-Ready). The three rubric checks are all real and mechanical: `hook-events.jsonl` is genuinely written by `_lib.py`'s `append_jsonl()`/`log_event()` from three different hooks (1500+ real lines, 2026-07-04 → 2026-07-21, with real result diversity including 16 `enforced_block` events); `ERRORS.md` has both `## Pending Review` and `## Active Lessons` populated with real, dated, non-placeholder entries; and `pr-retro/SKILL.md`'s "Case C" defines an explicit trigger for when a skill/agent file should be edited, routed through `ERRORS.md` Pending Review. What's missing for the next tier up: nothing currently reads `hook-events.jsonl` to automatically surface or propose a skill edit — a human/agent has to notice the pattern and manually run the pr-retro Case-C path; there's also no automated before/after trigger-rate validation loop wired into CI (the manual dispatch-and-compare procedure in `skill-creator-plus/references/eval-loop.md` and `harness-maintenance.md` §6.3 exists but is prose-guided, not scripted). The rollout-telemetry foundation is genuinely solid — the gap to L5 is entirely in automating the "log → decide → edit → validate" loop, not in the logging itself.
